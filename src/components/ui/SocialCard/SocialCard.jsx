import style from './SocialCard.module.scss'

function SocialCard({ link, icon, bgClassName, title, description }) {
    return (
        <div className={style.CardWrapper}>
            Social card
        </div>
    )
}

export default SocialCard